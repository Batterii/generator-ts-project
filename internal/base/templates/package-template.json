{
	"name": "@batterii/<%= name %>",
	"version": "0.0.0",
	"description": "<%= description %>",
	"author": "Batterii, LLC",
	"license": "UNLICENSED",
	"repository": "github:Batterii/<%= name %>",
	"files": [],
	"scripts": {
		"generate": "yo --local-only",
		"generate:class": "yo --local-only @batterii/ts-class",
		"generate:module": "yo --local-only @batterii/ts-module",
		"generate:test": "yo --local-only @batterii/ts-test",
		"lint": "eslint . --ext .ts",
		"lintf": "eslint . --ext .ts --fix",
		"prebuild": "rm -rf dist",
		"build": "tsc",
		"preversion": "npm run lint",
		"prepublishOnly": "git push && git push --tags"
	}
}
